# The Ledger

Personal finance app — envelope budgeting with item-level tracking, debt payoff planning, and real-time free-cash visibility.

## Stack

- **Backend**: Node + TypeScript + Hono + Drizzle + better-sqlite3
- **Frontend**: React + Vite + TanStack Query
- **Database**: SQLite (single file in `./data/app.db`)
- **Validation**: Zod (shared between client and server)

## First-time setup

```bash
# Install dependencies
npm install

# Create the SQLite database from the schema
npm run db:push

# Start dev servers (backend on :3000, frontend on :5173)
npm run dev
```

Open `http://localhost:5173` — you should see "The Ledger".

## Project structure

```
src/
├── server/             # Hono routes, business logic
│   ├── index.ts        # Entry point
│   └── routes/         # One file per resource
│       ├── accounts.ts
│       ├── budget.ts
│       ├── categories.ts
│       ├── dashboard.ts
│       ├── payees.ts
│       ├── schedules.ts
│       ├── tags.ts
│       └── transactions.ts
├── client/             # React app
│   ├── App.tsx
│   ├── main.tsx
│   ├── components/
│   │   └── AddAccountModal.tsx
│   └── pages/
│       ├── AccountsPage.tsx
│       ├── BudgetPage.tsx
│       ├── DashboardPage.tsx
│       ├── DebtPage.tsx
│       ├── HelpPage.tsx
│       ├── LedgerPage.tsx
│       └── SchedulesPage.tsx
├── shared/             # Code used by both client and server
│   └── schemas.ts      # Zod schemas for runtime validation
└── db/
    ├── schema.ts       # Drizzle schema (source of truth)
    ├── client.ts       # DB connection + startup backup
    └── migrations/     # Generated by drizzle-kit
```

## Useful commands

| Command | What it does |
|---|---|
| `npm run dev` | Run server + client with hot reload |
| `npm run db:push` | Sync schema changes to SQLite |
| `npm run db:studio` | Open Drizzle Studio (DB browser) at `local.drizzle.studio` |
| `npm run typecheck` | Check types without building |
| `npm run build` | Production build |
| `npm start` | Run production server |

---

## Feature overview

### Dashboard

Displays **Unassigned Cash** — how much money in your on-budget asset accounts is not yet spoken for by a budget envelope or an upcoming scheduled bill.

Three views:

| View | What it shows |
|---|---|
| **Right Now** | Cash balance − reserved envelopes − next-30-day scheduled outflows |
| **End of Month** | Same but uses end-of-month scheduled window |
| **Per Account** | Balance breakdown by individual cash account |

Color coding: **green** = healthy; **amber** = positive but trending down vs. last month; **red** = negative.

The right-side breakdown shows:
- **Cash Accounts** — total across checking / savings / cash subtypes
- **Reserved for Budget** — money locked in non-zero expense envelopes
- **Scheduled Transactions** — upcoming bills not yet covered by envelopes (within 30-day window)

---

### Debt Dashboard

Tracks all on-budget liability accounts with payoff projections.

**Summary bar** (top):
- Total Owed, Monthly Payment, Weighted Avg APR, Debt-Free date

**Per-account cards**:

| Column | Contents |
|---|---|
| **Loan Info** | APR, promotional rate banner (if applicable), min payment, due day |
| **Monthly Payment** | Editable budget amount — click to change, saves instantly, recalculates payoff live |
| **Payoff Projection** | Monthly interest, principal paid, paid-off date, total interest |

**Promotional APR support**: When an account has a promotional rate (e.g., 0% for 12 months), the payoff projection runs a **two-phase calculation**:
1. Payments at the promo rate until the promo end date
2. Remaining balance at the standard APR

The card shows:
- A countdown badge ("7 months left")
- **To clear before promo ends** — the monthly payment needed to pay off the balance before the higher rate kicks in, with an "✓ on track" indicator
- A warning if the current payment leaves a balance that will switch to the standard APR, with the projected remaining balance

**Setting a promotional rate**: Go to **Accounts** → Add or Edit a liability → change **Rate Type** to "Promotional" → fill in Promo APR, Standard APR, and Promo End Date.

**Debt categories**: Each liability account gets an automatically created "Bank Card Debt – [Name]" category in a "Debt Payments" group. These categories are hidden from the Budget page to keep it uncluttered — they live on the Debt page where you set the monthly payment amount directly on the card.

---

### Accounts

Lists all accounts (asset, liability, tracking) with live balances. Clicking an account navigates to its ledger.

**Adding a liability account**:
1. Click **+ Add Account**
2. Set Type → Liability
3. Fill in Subtype (credit card, mortgage, student loan, etc.)
4. Enter Starting Balance (the amount you currently owe — entered as a positive number)
5. Optionally fill in **Debt Details**: rate type, APR, credit limit, min payment, statement day, due day

A "Bank Card Debt" budget category is auto-created and linked to the account.

---

### Budget

Envelope-style budget with month navigation. Each category shows:
- **Assigned** — money you've allocated to this envelope this month
- **Activity** — spending against this category
- **Available** — running balance (carries over or resets depending on category settings)

**Ready to Assign** (top right) = total on-budget asset balances − total non-zero envelope balances. Debt categories are excluded from the budget page — manage debt payments on the Debt page.

---

### Ledger

Full transaction history for a single account, filterable and sortable. Supports:
- Manual transaction entry
- Split transactions (one transaction, multiple categories)
- Tags
- Cleared / reconciled status
- Transfer entry between accounts

---

### Schedules

Recurring transaction rules using RRULE (weekly, monthly, every N days, etc.). Scheduled transactions appear as upcoming outflows on the Dashboard before they're posted. Supports auto-posting.

---

### Reconciliation

Accessible per-account from the Accounts page. Enter your statement balance → the app computes any adjustment needed and locks cleared transactions as reconciled.

---

## Key design decisions

**Liability accounts and RTA**: Liability account balances are excluded from the Ready to Assign calculation. Only asset (checking, savings, cash, investment) balances count as "money you have." Debt is tracked separately on the Debt page.

**Debt categories off the Budget page**: Debt payment categories are automatically created but filtered out of the Budget view. This keeps the budget focused on spending decisions. The Debt page is the single place to view and adjust debt payment amounts.

**Starting balances for liabilities**: When a liability account is created with a non-zero starting balance, the server creates a categorized transaction against the account's debt category (rather than storing the balance directly on the account). This ensures the debt is properly reflected in category activity.

**APR storage**: APR values are stored as decimal fractions (e.g., `0.2499` = 24.99%). Multiply by 100 for display; use as-is in interest math (`balance × apr / 12`).

**Backups**: The database is backed up automatically on server startup (once per day) and before any schema migrations. Backups are kept in `./data/backups/`, capped at 30 files.
